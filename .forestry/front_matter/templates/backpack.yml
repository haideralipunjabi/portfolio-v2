---
label: Backpack
hide_body: true
fields:
- name: backpack
  type: field_group_list
  fields:
  - name: name
    type: text
    config:
      required: false
    label: Name
  - name: icon
    type: file
    config:
      maxSize: 64
    label: Icon
  config:
    min: 
    max: 
    labelField: 
  label: Backpack
pages:
- data/backpack.json
